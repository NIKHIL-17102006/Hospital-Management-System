# First Normal Form (1NF)

Changes from UNF:
- Removed repeating groups (multiple medicines and lab tests)
- Ensured atomic values (each field contains a single value)

Now:
- Each row represents a single record
- No multi-valued attributes exist

This is the first step towards normalization.
